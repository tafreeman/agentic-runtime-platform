# Generates CycloneDX SBOMs for all packages. Implements EO 14028 §4 (software supply chain security) and NIST SP 800-218 SSDF PW.4.2.

name: SBOM Generation

on:
  push:
    branches: [main]
  workflow_dispatch:

# Opt into Node.js 24 for JS-based actions (actions/checkout, setup-python).
# The Node 20 default is being removed from runners on 2026-09-16.
env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  generate-sbom:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v6

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.11"

      - name: Set up Node
        uses: actions/setup-node@v6
        with:
          node-version: "20"

      - name: Install Python packages
        run: |
          python -m pip install --upgrade pip
          pip install -e "."
          pip install -e "agentic-workflows-v2/[dev]"
          pip install -e "agentic-v2-eval/[dev]"

      - name: Install cyclonedx-bom
        run: pip install cyclonedx-bom

      - name: Create SBOM output directory
        run: mkdir -p sbom

      - name: Generate Python SBOM
        run: cyclonedx-py environment --output-format XML --output-file sbom/python-sbom.xml

      - name: Install Node UI dependencies
        working-directory: agentic-workflows-v2/ui
        run: npm ci

      - name: Generate Node SBOM
        working-directory: agentic-workflows-v2/ui
        run: |
          npx --yes @cyclonedx/cyclonedx-npm \
            --output-format XML \
            --output-file ../../sbom/node-sbom.xml \
          || {
            echo "npx @cyclonedx/cyclonedx-npm failed — falling back to package-lock.json artifact"
            cp package-lock.json ../../sbom/node-package-lock.json
          }

      - name: Upload SBOM artifacts
        uses: actions/upload-artifact@v7
        with:
          name: sbom-${{ github.sha }}
          path: sbom/
          retention-days: 90
