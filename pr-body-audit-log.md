🤖 Generated by the Test Improver run on 2026-07-31 (AI-assisted; validated by automated evidence per `AI_EVIDENCE_STANDARD.md`).

## What

Adds `tests/test_audit_log_failure_paths.py` — 16 tests for the failure and recovery paths of `agentic_v2/server/audit_log.py`. Test-only change; no runtime code touched.

## Why

The tamper-evidence property was only asserted on the happy path. Untested until now:

- `verify_audit_chain` **rejection** paths — mutated payload, deleted first record, reordered records
- `FileAuditStore` tail recovery — chain resume after restart; corrupt/blank/hashless tails degrade to `None`
- `RedisAuditStore` degradation — no-op when unconnected, demotion on `OSError` during `XADD`, `get_last_hash` fallback to serialized record, `close()` cleanup when `aclose()` raises
- `build_audit_logger` redis→file fallback and connected-store selection
- `audit_request_event` never-raise contract + request-id/client-ip capture
- `_json_safe` coercion, `_decode_redis_value` bytes handling

## Coverage impact

| Scope | Before | After |
|---|---|---|
| `agentic_v2/server/audit_log.py` (260 stmts) | 61.95% | **84.91%** |

Remaining misses are the redis/structlog import-absent fallbacks and live-connection paths.

## Evidence

- 16/16 pass standalone; green with `tests/test_audit_log.py` and the full `tests/server` suite in **both** collection orders (module loader mirrors the `setdefault` shim from `test_audit_log.py`, per its ADR-023 B-1 comment)
- Full fast suite at base: 4,139 passed, 0 failed; combined gate scope 84.44%
- `ruff check` and `black --check` clean; no `os.environ` writes (stubs + `monkeypatch` only); no new dependencies
