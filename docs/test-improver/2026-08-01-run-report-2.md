# 🤖 Daily Test Improver — run report (second run of 2026-08-01)

**Repo:** `agentic-runtime-platform` · **Branch:** `adr_016` (`71ee65ae`) ·
**Follows:** `2026-08-01-run-report.md` (08:21 run)
**AI transparency:** this report and the changes below were produced by an
automated assistant. No commit, push, PR, or issue comment was made; changes
are left in the working tree for review.

**Deliberately small run.** Four test-improver test files plus an 11-line
production diff already sit uncommitted awaiting your review. Rather than adding
a fifth large suite, this run validated the pipeline end to end, knocked out the
two cheap July backlog items, and re-measured the baseline the next run should
plan against.

---

## 1. Commands discovered and validated

Executed in a fresh Linux sandbox venv (Python 3.11.15, uv-provisioned, CI's
install set pinned to `ci-constraints.txt`; the repo's Windows `.venv` was not
touched).

| Purpose | Command | Result |
|---|---|---|
| Unit suite (CI's marker filter, `AGENTIC_NO_LLM=1`) | `pytest tests/ -q -m "not integration and not slow" --ignore=tests/e2e --timeout=120` | ✅ **4,269 passed, 0 failed**, 13 skipped, 2 xfailed (10 shards) |
| Coverage aggregate | `coverage report --fail-under=80` on combined data | ✅ **85.50%** (21,928 stmts, gate 80.00) |
| Lint | `ruff check` on changed files | ✅ clean |
| Format | `black --check` on changed files | ✅ clean |
| Suppression ratchet | `python scripts/check_suppression_ratchet.py` | ✅ all counters at baseline |
| Docs refs | `python agentic-workflows-v2/scripts/check_docs_refs.py` | ✅ no broken local references |
| Doc stats | `python scripts/generate_doc_stats.py --check` | ⚠️ hangs in this sandbox (see §2a) — computation replicated externally, drift fixed |

Test-count sanity: 4,269 = July run 2's 4,267 + the 2 tests added below.

## 2. Implemented: `MockBackend` now records sampling parameters

July backlog item 3, the smallest durable win available.
`MockBackend.complete` and `complete_chat`
(`agentic_v2/models/backends.py`) recorded `model`, `messages`, `tools`, and
`**kwargs` in `call_history` — but `max_tokens` and `temperature` bind to named
parameters, so **no test could ever assert on them through the backend**. Tests
that cared (e.g. `TestAgent`'s pinned `temperature=0.2`) had to monkeypatch the
client seam instead.

- **Source:** +4 lines — both methods now record `max_tokens` and `temperature`.
- **Tests:** `tests/test_model_backends.py` — the two existing history tests now
  assert the recorded defaults, and two new tests
  (`test_complete_records_sampling_params`,
  `test_complete_chat_records_sampling_params`) pin explicit values.
- **Safety check:** every existing `call_history` assertion in the suite is
  key-by-key, not exact-dict, so the additive keys break nothing — verified by
  the full-suite pass above.
- **Consistency:** the now-stale docstring in
  `tests/test_test_agent_pipeline.py::test_requests_low_temperature` (which
  documented the old limitation) was updated. That file is itself still
  untracked from the July run.

### 2a. Kept `just docs` green: stat-strip regeneration

`docs/index.md`'s "Backend tests" stat is a generated literal
(`scripts/generate_doc_stats.py`, AST count of `test_*` functions across the
three pytest roots) and the `--check` form gates CI via `just docs`. Adding two
tests makes that count drift: quoted **4,154**, recomputed **4,156**. The
script itself cannot run here — it hangs inside `Path.rglob` → `os.scandir` on
the sandbox's FUSE mount (interrupt traceback confirms; not a repo bug) — so
the count was replicated exactly (same roots, same `__pycache__` exclusion,
same AST predicate; 240 files) and the single generated literal in
`docs/index.md` was updated to 4,156, which is what regenerate mode would
write. ADR count (50), workflow count (6), and the README coverage badge (80%)
are unaffected. Re-run `python scripts/generate_doc_stats.py --check` on the
real machine to confirm.

## 3. Backlog item 4 closed: `implementations/` success-field audit — clean

July's `success=`-omission bug (`TestAgent`/`ArchitectAgent` raising
`ValidationError` on every run) prompted a sweep of the
`agents/implementations/` subpackage, which is coverage-omitted and was not
checked then. Result: **no further instances.** The only `TaskOutput`
construction is `claude_agent.py:304`, which passes `success=True` explicitly;
`claude_sdk_agent.py` and `agent_loader.py` construct no output models.

## 4. Correction to this morning's report

`2026-08-01-run-report.md` §6 names `server/audit_log.py` at **61.95%** as the
next coverage target. That number is stale — it is the *pre*-July-run-1 figure.
With `tests/test_audit_log_failure_paths.py` (untracked, July run 1) collected,
this run measures `audit_log.py` at **84.91%**. Do not spend a run on it.

The morning run also reported ~3,997 tests vs. the 4,269 measured here with an
identical filter; its 11-slice sharding most plausibly dropped a file or two,
which would also explain the stale audit-log figure. Same lesson as July run 1:
**confirm a module's test file situation before acting on its coverage row.**

## 5. Re-measured backlog (fresh single-source data, worst first)

Scope: non-omitted `agentic_v2`, known false gaps excluded.

| Priority | Module | Stmts | Miss | Cover | Notes |
|---|---|---|---|---|---|
| P1 | `agents/coder.py` | 117 | 51 | 51.59% | Flagged since July; extraction/error-handling blocks; placeholder-model testable |
| P2 | `integrations/metrics.py` | 135 | 46 | 59.12% | |
| P2 | `server/routes/runs.py` | 228 | 65 | 66.79% | Route error branches |
| P2 | `models/weight_integrity.py` | 216 | 50 | 72.50% | Integrity-verification code — failure paths are the point |
| P3 | `server/middleware/rate_limit.py` | 43 | 15 | 60.78% | Small, security-adjacent, cheap |
| P3 | `server/spa.py` | 26 | 10 | 53.33% | Tiny |
| P3 | `tools/builtin/build_ops.py` | 175 | 50 | 65.40% | |
| P3 | `engine/executor.py` | 266 | 70 | 69.27% | Core path — edge-case tests only, not line-chasing |
| P3 | `agents/reviewer.py` / `orchestrator.py` | 96/289 | 30/72 | 66.07%/72.68% | |

Known non-gaps, do not target: `scoring/evalkit_bridge.py` 0% (tests exist,
`importorskip` by design per ADR-042); `rag/vectorstore.py` 48.45% (LanceDB
class behind the uninstalled `rag` extra).

Current cleaned-tree baselines for drift tracking: **85.50%** with all four
untracked test files collected; July committed-tree reference was 84.44%.

## 6. Suggested actions

1. **Work through the review queue** — it is now the bottleneck, not coverage:
   `test_audit_log_failure_paths.py`, `test_test_agent_pipeline.py`,
   `test_redis_circuit_breaker_resilience.py`, `test_runtime_docker.py`, the
   11-line agents `success=` fix, and this run's 4-line `backends.py` change.
   Draft PR bodies for the first two already exist at the repo root
   (`pr-body-*.md`).
2. **Next run's implementation target:** `agents/coder.py` (P1) — unless the
   review queue is still this deep, in which case another skip-sized run is the
   right call.
3. Standing maintainer decisions are consolidated in
   `activity-2026-08.md` §Decisions.
